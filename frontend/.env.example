# SuperTokens client-side points to Next.js (rewrites proxy to backend)
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_SUPERTOKENS_API_DOMAIN=http://localhost:3000
NEXT_PUBLIC_SUPERTOKENS_WEBSITE_DOMAIN=http://localhost:3000
# Server-side rewrite destination — the actual backend
BACKEND_URL=http://localhost:3001
